
the mtd-utils need zlib 
	
	so, untar the zlib-1.2.8.tar.gz

	cd zlib-1.2.8

	./configure --shared --prefix=/usr

	maek 

	sudo make install


then compile the mtd-utils

	untar the mtd-tuils.tar.bz2

	cd mtd-utils dir

	make 

	sudo make instal


Or

	sudo apt-get install mtd-utils


						ZHAOCHAO
							 2016-8-28 
							
